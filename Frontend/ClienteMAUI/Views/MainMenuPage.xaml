<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ClienteMAUI.Views.MainMenuPage"
             Title="Menú Principal">

    <ScrollView>
        <VerticalStackLayout Padding="15" Spacing="20">

            <!-- Sección Categorías -->
            <ScrollView Orientation="Horizontal">
                <HorizontalStackLayout x:Name="CategoryButtonsLayout" Spacing="10"></HorizontalStackLayout>
            </ScrollView>


            <HorizontalStackLayout HorizontalOptions="Center" Spacing="20">
                <Button Text="Crear producto" 
                x:Name="btnCrearProducto"
                Clicked="OnCrearProductoClicked"
                BackgroundColor="#007AFF"
                TextColor="White"
                CornerRadius="10"
                Padding="12,6" />

                <Button Text="Ver subastas" 
                x:Name="btnVerSubastas"
                Clicked="OnVerSubastasClicked"
                BackgroundColor="#007AFF"
                TextColor="White"
                CornerRadius="10"
                Padding="12,6" />

                <!--Abraham-->
                <Button Text="Mi perfil"
                        x:Name="btnPerfil"
                        Clicked="NavigateToProfilePage"
                        BackgroundColor="#80000000"
                        TextColor="Black"
                        CornerRadius="12"/>
            </HorizontalStackLayout>


            <!-- Sección Lista de productos -->
            <CollectionView x:Name="ProductsCollection"
                            ItemsLayout="VerticalGrid, 2"
                            SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Stroke="LightGray" StrokeThickness="1" Padding="10" Margin="5" StrokeShape="RoundRectangle 10">
                            <VerticalStackLayout>
                                <Image Source="{Binding ImageSource}" HeightRequest="100" Aspect="AspectFill"/>
                                <Label Text="{Binding Nombre}" FontAttributes="Bold" FontSize="16" />
                                <Label Text="{Binding Precio, StringFormat='${0:F2}'}" FontSize="14" />
                                <Label Text="{Binding CantidadVendidos, StringFormat='Vendidos: {0}'}" FontSize="12" />

                                <!-- Mostrar botones solo si el producto es propio -->
                                <ContentView IsVisible="False">
                                    <ContentView.Triggers>
                                        <DataTrigger TargetType="ContentView"
                         Binding="{Binding EsPropio}"
                         Value="True">
                                            <Setter Property="IsVisible" Value="True" />
                                        </DataTrigger>
                                    </ContentView.Triggers>

                                    <HorizontalStackLayout x:Name="OpcionesLayout" Spacing="10">
                                        <Button Text="Modificar" BackgroundColor="Orange" TextColor="White" 
                    Clicked="OnModificarProductoClicked"/>
                                        <Button Text="Eliminar" BackgroundColor="Red" TextColor="White" 
                    Clicked="OnEliminarProductoClicked"/>
                                    </HorizontalStackLayout>
                                </ContentView>

                            </VerticalStackLayout>

                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>


