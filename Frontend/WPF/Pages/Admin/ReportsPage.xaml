<Page x:Class="WpfApp.Pages.Admin.ReportsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp.Pages.Admin"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ReportsPage">

    <Border Background="{StaticResource BackgroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Name="Btnback"
                        Style="{DynamicResource BackButtonStyle}"
                        Click="Btnback_Click"/>
                <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Center"
                            Margin="5" Grid.Column="1">
                    <TextBlock Text="{DynamicResource Reports_Header}"
                            Style="{DynamicResource HeaderLabelStyle}"
                               x:Name="PageHeader"/>
                    <TextBlock Text="{DynamicResource Reports_Desc}"
                               Style="{DynamicResource DescriptionLabelStyle}"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Page>
